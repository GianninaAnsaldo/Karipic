databases:
  - name: karipic
    databaseName: karipic_development
    user: karipic
    plan: free

services:
  - type: web
    name: karipic
    runtime: ruby
    plan: free
    buildCommand: "./bin/render-build.sh"
    # preDeployCommand: "bundle exec rails db:migrate" # preDeployCommand only available on paid instance types
    startCommand: "bundle exec rails server"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: postgresql://karipic_vmnp_user:Z4Ao1BiYL9fH6pXKm0Y3NF12mrMzqMOI@dpg-ctoutcdds78s73cuc16g-a/karipic_vmnp
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false
      - key: WEB_CONCURRENCY
        value: 2 # sensible default